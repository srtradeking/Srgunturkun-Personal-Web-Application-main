// This file is deprecated - using Vite instead of Vue CLI
// Vue.config.js is kept for reference only and should not be used
// All configuration is now in vite.config.js

// To use Vite:
//   npm run dev     → Vite dev server (http://localhost:8180)
//   npm run build   → Vite build (creates dist/)
//   npm run preview → Vite preview (http://localhost:4173)

// Legacy Vue CLI scripts (if needed):
//   npm run serve   → Vue CLI dev server (deprecated, use: npm run dev)
//   npm run lint    → ESLint (still available)

module.exports = {}
